import { NarudzbinaPiceDTO } from './narudzbina-pice-dto';

describe('NarudzbinaPiceDTO', () => {
  it('should create an instance', () => {
    expect(new NarudzbinaPiceDTO()).toBeTruthy();
  });
});
